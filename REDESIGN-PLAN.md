# 리뉴얼 기획안 — "차분함"에서 "인터랙티브 크래프트"로

방향 전환: 모노크롬 미니멀 → **절제된 3D/커서 인터랙션이 있는 포트폴리오**.
톤은 여전히 프론트엔드 개발자답게 정제되어 있되, 핵심 구간에서 "이 사람 진짜 잘 만드네"를 느끼게 하는 포인트 이펙트를 넣는다.

---

## 1. 문제 진단

### 1-1. 레이아웃이 불규칙한 실제 원인

섹션마다 좌측 여백 기준이 다르다 — 하나의 그리드 시스템이 없이 섹션별로 임기응변한 값:

| 컴포넌트 | 현재 좌측 패딩 | 문제 |
|---|---|---|
| AboutMe | `pl-40` | 고정값 |
| Projects | `pl-4 sm:pl-36 lg:pl-40` | About과 lg에서만 우연히 일치 |
| Skills | `pl-44 pr-10` | 혼자 다른 값 + 우측 패딩까지 별도 |
| Contact | `pl-40` + 좌측 패널 `w-[480px]` 하드코딩 | 폭이 컨텐츠가 아닌 매직넘버 |

→ 결과: 섹션을 넘길 때마다 콘텐츠 시작선이 미세하게 흔들림. 사용자는 "이유는 모르지만 어수선하다"고 느낀다.

Projects 카드 그리드도 `col-span-4/8/12` + `order-2/order-3` 반응형 재배치가 중첩되어 있어 브레이크포인트마다 다른 비율(인덱스 2 / 메타 3 / 프리뷰 3 → lg에서 2/5/5)로 흘러가는 것도 같은 원인.

### 1-2. 디자인이 심심한 이유

- 배경 단일 톤(`#0a0a0a`) + 텍스트 opacity 단계만으로 위계 표현 → 평면적
- accent 컬러 없음 → 클릭 가능한 요소와 아닌 요소의 시각적 긴장감 부족
- 모션이 hover 200ms 정도로만 제한되어 있어 스크롤/커서 움직임에 반응하는 요소가 전무
- Hero의 스크럽 비디오를 빼면 나머지 섹션은 정적 카드 나열

---

## 2. 방향

**컨셉명**: *Kinetic Craft* — 가만히 있으면 미니멀하지만, 마우스가 움직이는 순간 공간이 살아있다는 걸 보여준다.

- 레이아웃: 그리드를 하나로 통일해서 규칙을 세운다 (아래 3장)
- 비주얼: accent 컬러 1개 도입 + 섹션마다 미세하게 다른 텍스처로 단조로움 해소
- 인터랙션: **핵심 구간에만** 3D/커서 반응 이펙트 집중 배치 (전면 실험적 아님 — 절제된 포인트, 사용자 선택)

---

## 3. 레이아웃 통일 (우선순위 1)

전역 상수 도입, 모든 섹션이 여기서만 값을 가져온다.

```css
:root {
  --content-pl: clamp(1.5rem, 8vw, 11rem); /* 사이드바 폭 고려한 좌측 시작선 고정 */
  --content-pr: clamp(1.5rem, 5vw, 4rem);
  --content-max: 1400px;
}
```

- AboutMe / Projects / Skills / Contact 전부 `pl-[var(--content-pl)] pr-[var(--content-pr)]`로 교체, 개별 `pl-40`/`pl-44`/`w-[480px]` 매직넘버 제거
- Contact 좌측 패널은 고정폭 대신 `max-w-md` 비율 기반으로 변경 (다른 섹션의 텍스트 블록 폭과 통일)
- Projects의 인덱스/메타/프리뷰 비율은 브레이크포인트마다 바뀌는 `order` 로직 대신 **고정 12컬럼 그리드**(`2 / 5 / 5`)를 md 이상에서 항상 유지하고, 모바일만 세로 스택으로 단순화 — 중간 사이즈에서 튀는 비율 제거

---

## 4. 비주얼 임팩트

| 항목 | 현재 | 변경 |
|---|---|---|
| accent | 없음 | 저채도 1개 (예: `#6ee7ff` 계열 시안) — 링크 hover, 활성 인덱스, 커서 트레일에만 사용 |
| 배경 | 단일 `#0a0a0a` | 섹션 전환 시 아주 미세한 radial gradient 위치 이동 (마우스 위치 따라, opacity 3~5% 수준 — "그라디언트 남발" 금지선은 지키되 완전 평면 탈피 |
| 타이포 | 헤딩 `text-3xl` 균일 | Hero/섹션 타이틀에 `clamp()` 기반 대형 타이포 도입, 숫자(연도·인덱스)는 모노스페이스로 대비 강화 |

---

## 5. 3D / 커서 인터랙션 — 절제된 포인트 배치

"전면 실험적"이 아니라 **섹션당 1개, 총 4곳**에만 집중한다. 나머지는 기존 담백한 hover 유지.

1. **Hero 로고** — 마우스 위치에 따라 `rotateX/rotateY` 최대 6~8도 틸트 (`useSpring`, stiffness 100 / damping 14). 이미 있는 스크럽 애니메이션은 그대로 두고, 정지 상태(로고 오버레이 보이는 구간)에서만 적용
2. **Projects 프리뷰 iframe 카드** — 카드에 `perspective` 부여, 마우스 진입 시 `rotateY` 3~5도 + `translateZ(10px)` 살짝 떠오르는 느낌. 브라우저 크롬(점 3개) 바는 그대로 살려서 "실제 창을 기울여 보는" 느낌
3. **Skills 아이콘 그리드** — 커서에서 가까운 아이콘일수록 `scale(1.08)` + 살짝 떠오름 (반경 120px 이내만 반응, `useSpring`으로 자연스럽게) — 마우스가 지나가면 아이콘들이 반응하는 잔잔한 재미
4. **Sidebar 섹션 인디케이터** — 활성 점(dot)에서 다음 섹션 점으로 이동할 때 clip-path 기반 모핑 트레일 (직선 이동이 아니라 살짝 늘어졌다 줄어드는 형태)

**하지 않는 것** (이번 리뉴얼에서도 유지):
- 키보드/자동 트리거 애니메이션에 모션 추가 금지 (스크롤·클릭 등 사용자 행동에만 반응)
- 300ms 넘는 UI 애니메이션 금지 (단, 장식용 스프링은 지속시간 개념이 없으므로 예외)
- 전체 배경에 파티클/노이즈 캔버스 같은 무거운 전면 이펙트는 넣지 않음 (Playwright 확인한 실제 성능 예산 고려)

---

## 6. 실행 순서 (제안)

1. **레이아웃 통일** — 그리드 변수 도입, 매직넘버 제거 (가장 먼저, 눈에 띄는 "불규칙함"부터 해결)
2. **컬러/타이포 임팩트** — accent 도입, 타이틀 스케일 조정
3. **포인트 이펙트 4곳** — Hero 틸트 → Projects 카드 → Skills 커서 반응 → Sidebar 트레일 순으로 하나씩 추가하고 매번 Playwright로 확인
4. **DESIGN.md 갱신** — 최종 확정된 값(그리드 변수, accent 컬러, 모션 스펙)을 문서에 반영

각 단계 끝날 때마다 스크린샷으로 확인하고 다음 단계로 넘어가는 걸 추천드립니다 (한번에 다 바꾸면 뭐가 문제였는지 찾기 어려움).

---

## 7. 결정 필요한 것

- accent 컬러 정확한 값 (제안: 시안 계열 `#6ee7ff`, 다른 색 선호 있으면 교체)
- Contact 폼 추가 여부 (현재는 링크 나열만 — 그대로 유지 vs 간단한 폼 추가)

이 방향대로 진행할까요, 아니면 특정 구간(예: 3D 효과 배치)을 조정하고 싶으신가요?
